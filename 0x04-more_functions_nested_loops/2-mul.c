#include <stdio.h>
/**
 * mul(int a, int b)- multiplication of two numbers
 *
 * @a: first number to be multiplied
 *
 * @b: second number to be multiplied
 *
 * Return: a*b (always success)
*/
int mul(int a, int b)
{
	return (a * b);
}
