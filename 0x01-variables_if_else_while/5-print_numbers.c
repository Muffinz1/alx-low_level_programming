#include <stdio.h>
/**
 * main - entry point
 *
 * Description: printf numbers
 *
 * Return: 0 (always success)
*/

int main(void)
{	int X = 0;
	while (X <= 9)
	{
		printf("%i", X);
		X++;
	}
	printf("\n");
	return (0);
}
