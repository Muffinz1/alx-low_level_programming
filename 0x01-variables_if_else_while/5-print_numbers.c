#include <stdio.h>
/**
 * main - entry point
 *
 * Description: for char put
 *
 * Return: 0 (always success)
*/

int main(void)
{	int X = 0;
	while (X <= 0)
	{
		printf("%i", X);
		X++;
	}
	printf("\n");
	return (0);
}
