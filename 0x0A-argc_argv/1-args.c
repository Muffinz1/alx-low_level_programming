#include <stdio.h>
/**
 * main - entry point for agrc values
 *@argc: arguement count
 *@argv: arguement array values
 * Return: 0 (always success)
*/

int main(int argc, char **argv)
{
	void(**argv);

	printf("%s\n", argc - 1);
	return (0);
}
